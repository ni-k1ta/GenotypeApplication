namespace GenotypeApplication.Constants
{
    public static class AppConstants
    {
        public static readonly int MAX_CORES = Environment.ProcessorCount;

        public const string PROJECT_FILE_EXTENSION = ".aproj";
        public const string CONFIGURATION_FILES_EXTENSION = ".cfg";

        //public static readonly string[] ADDITIONAL_PROGRAMS = ["Structure", "Structure Harvester", "CLUMPP", "Distruct"];
        public const int MAX_RECENT_PROJECTS = 10;
    }
}
